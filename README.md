# SpecRelay Runner

The SpecRelay **execution plane**: a standalone runner client that claims
approved work from the Platform runner API and executes it on a machine you
control.

Content is being extracted from the development workspace under
`MVP-0015-runner-repository-extraction`.
